{
  "name": "mcp-scripts",
  "version": "1.0.0",
  "description": "TypeScript scripts for MCP code mode",
  "type": "module",
  "scripts": {
    "build": "tsup scan/**/*.ts tests/**/*.ts --format esm --dts --clean",
    "build:watch": "tsup scan/**/*.ts tests/**/*.ts --format esm --dts --watch"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "tsup": "^8.5.0",
    "typescript": "^5.9.3"
  }
}
