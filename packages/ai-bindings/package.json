{
  "name": "ai-bindings",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --tsconfig ./tsconfig.build.json"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
